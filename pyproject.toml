[project]
name = "mcp-sales-tools"
version = "0.1.0"
dependencies = [
    "mcp",
    "databricks-vectorsearch",
    "python-dotenv",
    "databricks-sdk"
]

[project.scripts]
custom-server = "server.main:main"
